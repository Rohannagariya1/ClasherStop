"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.GetJsonFormat = void 0;
class GetJsonFormat {
    formatLog(info) {
        const logObject = {
            timestamp: info.timestamp,
            level: info.level
        };
        if (info.traceId) {
            logObject.traceId = info.traceId;
        }
        if (info.IPAddress) {
            logObject.IPAddress = info.IPAddress;
        }
        if (info.parsedStack) {
            logObject.trace = info.parsedStack ? info.parsedStack : '';
        }
        if (info.context) {
            logObject.context = info.context || '';
        }
        if (info.id) {
            logObject.id = info.id || '';
        }
        logObject.message = info.message;
        return JSON.stringify(logObject);
    }
}
exports.GetJsonFormat = GetJsonFormat;
